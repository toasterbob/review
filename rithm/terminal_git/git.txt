git - version control system - VCS
git - lists commands

git init - initializes git
git and github two independent things

you can use git locally on your machine

git status - gives current state of git
git log
git diff -

git commit - goes into vim - hit i to insert - write long message - hit esc - :wq

go back in time - in git

git checkout HEAD~2 - go back two commits
git checkout master - to go back to current
git checkout 3bced1dfee48459bfd1b1f2cd3d7029ed177a457 - paste in sha to go to that point

git remote -v
